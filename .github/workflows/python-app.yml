name: Python application

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.9
      uses: actions/setup-python@v2
      with:
        python-version: "3.9"
    - name: Install dependencies
      run: |
        pip install pipenv
        pipenv install
        pipenv install requires.io
        pipenv run pwd
        pipenv run ls -al
        pipenv run requires.io update-branch -t ${{ secrets.REQUIRESIO }} -r video_management -n ${{ github.ref_name }} ./Pipfile.lock
        pipenv install --dev
    - name: Test with pytest
      run: |
        pytest tests/unit
        pytest tests/integration
